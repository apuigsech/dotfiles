- defaults:
    link:
      relink: true

- link:
    ~/.gitconfig: .gitconfig
    ~/.gitconfig.d: .gitconfig.d
    ~/.gitignore: .gitignore
    ~/bin/git-bulk:
      path: bin/git-bulk
      mode: 0750
