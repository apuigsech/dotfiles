# AWS CLI aliases
# Note: 'op plugin run' wrapper is intentionally NOT used here.
# The ontopix-dev profile uses credential_process in ~/.aws/config which
# calls 'op item get' directly. The op plugin run wrapper conflicts with
# credential_process (see https://github.com/1Password/shell-plugins/issues/213).
