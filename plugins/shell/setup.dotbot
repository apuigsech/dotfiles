- defaults:
    link:
      relink: true
      force: true

- create:
    - ~/.shell

- link:
    ~/.aliases: .aliases
    ~/.functions: .functions
    ~/.env: .env
    ~/.paths: .paths
    ~/.shellrc: .shellrc
    ~/bin/aws-op-credentials:
      path: bin/aws-op-credentials
      mode: 0750
