- defaults:
    link:
      relink: true
      force: true

- create:
    - ~/.shell

- link:
    ~/.aliases: .aliases
    ~/.functions: .functions
    ~/.env: .env
    ~/.paths: .paths
    ~/.shellrc: .shellrc
